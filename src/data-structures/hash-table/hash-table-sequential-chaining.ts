//Seperate chaining - > array with linked list -> small table with collision support by linked list  -> lambda should be around 1

import { HashTableOperations } from "./hash-table";

export class HashTableSequentialChaining<T> implements HashTableOperations<T>{
    insert = (data: T) =>{

    }
    remove = (data: T) => {
        return undefined;
    }
    find = (data: T) => {
        return undefined;
    }
}